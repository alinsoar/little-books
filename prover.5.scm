
(load "prover-load-j-bob.scm")


(jbob.prove (prelude)
            '(((defun memb? (xs)
                 (if (atom xs)
                     'nil
                     (if (equal (car xs) '?)
                         't
                         (memb? (cdr xs)))))
               (size xs))))

(jbob.prove (prelude)
            '(((defun memb? (xs)
                 (if (atom xs)
                     'nil
                     (if (equal (car xs) '?)
                         't
                         (memb? (cdr xs)))))
               (size xs)
               ((Q) (natp/size xs))
               )))

(jbob.prove (prelude)
            '(((defun memb? (xs)
                 (if (atom xs)
                     'nil
                     (if (equal (car xs) '?)
                         't
                         (memb? (cdr xs)))))
               (size xs)
               ((Q) (natp/size xs))
               ((A E E) (size/cdr xs))
               )))

(jbob.prove (prelude)
            '(((defun memb? (xs)
                 (if (atom xs)
                     'nil
                     (if (equal (car xs) '?)
                         't
                         (memb? (cdr xs)))))
               (size xs)
               ((Q) (natp/size xs))
               ((A E E) (size/cdr xs))
               ((A E) (if-same (equal (car xs) '?) 't)))))

(jbob.prove (prelude)
            '(((defun memb? (xs)
                 (if (atom xs)
                     'nil
                     (if (equal (car xs) '?)
                         't
                         (memb? (cdr xs)))))
               (size xs)
               ((Q) (natp/size xs))
               ((A E E) (size/cdr xs))
               ((A E) (if-same (equal (car xs) '?) 't))
               ((A) (if-same (atom xs) 't))
               )))

(jbob.prove (prelude)
            '(((defun memb? (xs)
                 (if (atom xs)
                     'nil
                     (if (equal (car xs) '?)
                         't
                         (memb? (cdr xs)))))
               (size xs)
               ((Q) (natp/size xs))
               ((A E E) (size/cdr xs))
               ((A E) (if-same (equal (car xs) '?) 't))
               ((A) (if-same (atom xs) 't))
               (() (if-true 't 'nil))
               )))

(jbob.prove (defun.memb?)
            '(((defun remb (xs)
                 (if (atom xs)
                     '()
                     (if (equal (car xs) '?)
                         (remb (cdr xs))
                         (cons (car xs)
                               (remb (cdr xs))))))
               (size xs))))

(jbob.prove (defun.memb?)
            '(((defun remb (xs)
                 (if (atom xs)
                     '()
                     (if (equal (car xs) '?)
                         (remb (cdr xs))
                         (cons (car xs)
                               (remb (cdr xs))))))
               (size xs)
               ((Q) (natp/size xs)))))

(jbob.prove (defun.memb?)
            '(((defun remb (xs)
                 (if (atom xs)
                     '()
                     (if (equal (car xs) '?)
                         (remb (cdr xs))
                         (cons (car xs)
                               (remb (cdr xs))))))
               (size xs)
               ((Q) (natp/size xs))
               (() (if-true (if (atom xs)
                                't
                                (< (size (cdr xs)) (size xs)))
                            'nil)))))

(jbob.prove (defun.memb?)
            '(((defun remb (xs)
                 (if (atom xs)
                     '()
                     (if (equal (car xs) '?)
                         (remb (cdr xs))
                         (cons (car xs)
                               (remb (cdr xs))))))
               (size xs)
               ((Q) (natp/size xs))
               (() (if-true (if (atom xs)
                                't
                                (< (size (cdr xs)) (size xs)))
                            'nil))
               ((E) (size/cdr xs)))))

(jbob.prove (defun.memb?)
            '(((defun remb (xs)
                 (if (atom xs)
                     '()
                     (if (equal (car xs) '?)
                         (remb (cdr xs))
                         (cons (car xs)
                               (remb (cdr xs))))))
               (size xs)
               ((Q) (natp/size xs))
               (() (if-true (if (atom xs)
                                't
                                (< (size (cdr xs)) (size xs)))
                            'nil))
               ((E) (size/cdr xs))
               (() (if-same (atom xs) 't)))))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               )))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               )))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               ((1 1 Q) (atom '())))))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               ((1 1 Q) (atom '()))
               ((1 1) (if-true '()
                               (if (equal (car '()) '?)
                                   (remb (cdr '()))
                                   (cons (car '()) (remb (cdr '())))))))))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               ((1 1 Q) (atom '()))
               ((1 1) (if-true '()
                               (if (equal (car '()) '?)
                                   (remb (cdr '()))
                                   (cons (car '()) (remb (cdr '()))))))
               ((1) (memb? '())))))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               ((1 1 Q) (atom '()))
               ((1 1) (if-true '()
                               (if (equal (car '()) '?)
                                   (remb (cdr '()))
                                   (cons (car '()) (remb (cdr '()))))))
               ((1) (memb? '()))
               ((1 Q) (atom '())))))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               ((1 1 Q) (atom '()))
               ((1 1) (if-true '()
                               (if (equal (car '()) '?)
                                   (remb (cdr '()))
                                   (cons (car '()) (remb (cdr '()))))))
               ((1) (memb? '()))
               ((1 Q) (atom '()))
               ((1) (if-true 'nil (if (equal (car '()) '?)
                                      't
                                      (memb? (cdr '()))))))))

(jbob.prove (defun.remb)
            '(((dethm memb?/remb0 ()
                      (equal (memb? (remb '())) 'nil))
               nil
               ((1 1) (remb '()))
               ((1 1 Q) (atom '()))
               ((1 1) (if-true '()
                               (if (equal (car '()) '?)
                                   (remb (cdr '()))
                                   (cons (car '()) (remb (cdr '()))))))
               ((1) (memb? '()))
               ((1 Q) (atom '()))
               ((1) (if-true 'nil (if (equal (car '()) '?)
                                      't
                                      (memb? (cdr '())))))
               (() (equal-same 'nil)))))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil)))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '()))))))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '())))))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '())))))))))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '())))))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '())))))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               
               
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               ((1 E 1) (cdr/cons x1 '()))
               
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               ((1 E 1) (cdr/cons x1 '()))
               ((1 E) (memb? '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               ((1 E 1) (cdr/cons x1 '()))
               ((1 E) (memb? '()))
               ((1 E Q) (atom '()))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               ((1 E 1) (cdr/cons x1 '()))
               ((1 E) (memb? '()))
               ((1 E Q) (atom '()))
               ((1 E) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               ((1 E 1) (cdr/cons x1 '()))
               ((1 E) (memb? '()))
               ((1 E Q) (atom '()))
               ((1 E) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1) (if-same (equal x1 '?) 'nil))
               )))

(jbob.prove (dethm.memb?/remb0)
            '(((dethm memb?/remb1 (x1)
                      (equal (memb? (remb (cons x1 '()))) 'nil))
               nil
               ((1 1) (remb (cons x1 '())))
               ((1 1 E Q 1) (car/cons x1 '()))
               ((1 1 E A 1) (cdr/cons x1 '()))
               ((1 1 E E 1) (car/cons x1 '()))
               ((1 1 E E 2 1) (cdr/cons x1 '()))
               ((1) (if-same (atom (cons x1 '()))
                             (memb?
                              (if (atom (cons x1 '()))
                                  '()
                                  (if (equal x1 '?)
                                      (remb '())
                                      (cons x1 (remb '())))))))
               
               ((1 A 1) (if-nest-A (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 E 1) (if-nest-E (atom (cons x1 '()))
                                   '()
                                   (if (equal x1 '?)
                                       (remb '())
                                       (cons x1 (remb '())))))
               ((1 A) (memb? '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-same (equal x1 '?)
                               (memb? (if (equal x1 '?)
                                          (remb '())
                                          (cons x1 (remb '()))))))
               ((1 E A 1) (if-nest-A (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 E E 1) (if-nest-E (equal x1 '?)
                                     (remb '())
                                     (cons x1 (remb '()))))
               ((1 Q) (atom/cons x1 '()))
               ((1) (if-false 'nil
                              (if (equal x1 '?)
                                  (memb? (remb '()))
                                  (memb? (cons x1 (remb '()))))))
               ((1 E 1 2) (remb '()))
               ((1 E 1 2 Q) (atom '()))
               ((1 E 1 2) (if-true '() (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               ((1 A 1) (remb '()))
               ((1 A 1 Q) (atom '()))
               ((1 A 1) (if-true '()
                                 (if (equal (car '()) '?)
                                     (remb (cdr '()))
                                     (cons (car '()) (remb (cdr '()))))))
               ((1 A) (memb? '()))
               ((1 E) (memb? (cons x1 '())))
               ((1 E Q) (atom/cons x1 '()))
               ((1 A Q) (atom '()))
               ((1 A) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1 E) (if-false 'nil
                                (if (equal (car (cons x1 '())) '?)
                                    't
                                    (memb? (cdr (cons x1 '()))))))
               ((1 E Q 1) (car/cons x1 '()))
               ((1 E) (if-nest-E (equal x1 '?)
                                 't
                                 (memb? (cdr (cons x1 '())))))
               ((1 E 1) (cdr/cons x1 '()))
               ((1 E) (memb? '()))
               ((1 E Q) (atom '()))
               ((1 E) (if-true 'nil
                               (if (equal (car '()) '?)
                                   't
                                   (memb? (cdr '())))))
               ((1) (if-same (equal x1 '?) 'nil))
               (() (equal 'nil 'nil))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil)))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '()))))))))))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))

               ((1 E E) (memb? '()))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))

               ((1 E E) (memb? '()))

               ((1 E E Q) (atom '()))
               
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))

               ((1 E E) (memb? '()))

               ((1 E E Q) (atom '()))
               
               ((1 E E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))

               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))

               ((1 E E) (memb? '()))

               ((1 E E Q) (atom '()))
               
               ((1 E E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))

               ((1 E) (if-same (equal x2 '?) 'nil))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))

               ((1 E E) (memb? '()))

               ((1 E E Q) (atom '()))
               
               ((1 E E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))

               ((1 E) (if-same (equal x2 '?) 'nil))

               ((1) (if-same (equal x1 '?) 'nil))
               )))

(jbob.prove (dethm.memb?/remb1)
            '(((dethm memb?/remb2 (x1 x2)
                      (equal (memb? (remb (cons x2 (cons x1 '())))) 'nil))
               nil
               ((1 1) (remb (cons x2 (cons x1 '()))))
               ((1 1 Q) (atom/cons x2 (cons x1 '())))
               ((1 1) (if-false '()
                                (if (equal (car (cons x2 (cons x1 '()))) '?)
                                    (remb (cdr (cons x2 (cons x1 '()))))
                                    (cons (car (cons x2 (cons x1 '())))
                                          (remb (cdr (cons x2 (cons x1 '()))))))))
               ((1 1 Q 1) (car/cons x2 (cons x1 '())))
               ((1 1 A 1) (cdr/cons x2 (cons x1 '())))
               ((1 1 E 1) (car/cons x2 (cons x1 '())))
               ((1 1 E 2 1) (cdr/cons x2 (cons x1 '())))
               ((1) (if-same (equal x2 '?)
                             (memb?
                              (if (equal x2 '?)
                                  (remb (cons x1 '()))
                                  (cons x2 (remb (cons x1 '())))))))
               ((1 A 1) (if-nest-A (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 E 1) (if-nest-E (equal x2 '?)
                                   (remb (cons x1 '()))
                                   (cons x2 (remb (cons x1 '())))))
               ((1 A 1) (remb (cons x1 '())))
               ((1 E 1 2) (remb (cons x1 '())))
               ((1 A 1 Q) (atom/cons x1 '()))
               ((1 E 1 2 Q) (atom/cons x1 '()))
               ((1 A 1) (if-false '()
                                  (if (equal (car (cons x1 '())) '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 E 1 2) (if-false '()
                                    (if (equal (car (cons x1 '())) '?)
                                        (remb (cdr (cons x1 '())))
                                        (cons (car (cons x1 '())) (remb (cdr (cons x1 '())))))))
               ((1 A 1 Q 1) (car/cons x1 '()))
               ((1 E 1 2 Q 1) (car/cons x1 '()))
               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (memb?
                                  (if (equal x1 '?)
                                      (remb (cdr (cons x1 '())))
                                      (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
                                 (memb?
                                  (cons
                                   x2
                                   (if (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))))))
               ((1 A A 1) (if-nest-A (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A E 1 2) (if-nest-A (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E A 1) (if-nest-E (equal x1 '?)
                                     (remb (cdr (cons x1 '())))
                                     (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 E E 1 2) (if-nest-E (equal x1 '?)
                                       (remb (cdr (cons x1 '())))
                                       (cons (car (cons x1 '())) (remb (cdr (cons x1 '()))))))
               ((1 A A 1 1) (cdr/cons x1 '()))
               ((1 A E 1 2 1) (cdr/cons x1 '()))
               ((1 E A 1 1) (car/cons x1 '()))
               ((1 E A 1 2 1) (cdr/cons x1 '()))
               ((1 E E 1 2 1) (car/cons x1 '()))
               ((1 E E 1 2 2 1) (cdr/cons x1 '()))
               ((1) (if-same (equal x2 '?)
                             (if (equal x1 '?)
                                 (if (equal x2 '?)
                                     (memb? (remb '()))
                                     (memb? (cons x2 (remb '()))))
                                 (if (equal x2 '?)
                                     (memb? (cons x1 (remb '())))
                                     (memb? (cons x2 (cons x1 (remb '()))))))))
               ((1 A A) (if-nest-A (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 A E) (if-nest-A (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   (memb? (remb '()))
                                   (memb? (cons x2 (remb '())))))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   (memb? (cons x1 (remb '())))
                                   (memb? (cons x2 (cons x1 (remb '()))))))
               ((1 A A 1) (remb '()))
               ((1 A E 1 2) (remb '()))
               ((1 E A 1 2) (remb '()))
               ((1 E E 1 2 2) (remb '()))
               
               ((1 A A 1 Q) (atom '()))
               ((1 A E 1 2 Q) (atom '()))
               ((1 E A 1 2 Q) (atom '()))
               ((1 E E 1 2 2 Q) (atom '()))

               ((1 A A 1) (if-true '()
                                   (if (equal (car '()) '?)
                                       (remb (cdr '()))
                                       (cons (car '()) (remb (cdr '()))))))
               ((1 A E 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E A 1 2) (if-true '()
                                     (if (equal (car '()) '?)
                                         (remb (cdr '()))
                                         (cons (car '()) (remb (cdr '()))))))
               ((1 E E 1 2 2) (if-true '()
                                       (if (equal (car '()) '?)
                                           (remb (cdr '()))
                                           (cons (car '()) (remb (cdr '()))))))

               ((1 A A) (memb? '()))
               ((1 A E) (memb? (cons x1 '())))
               ((1 E A) (memb? (cons x2 '())))
               ((1 E E) (memb? (cons x2 (cons x1 '()))))

               ((1 A A Q) (atom '()))
               ((1 A E Q) (atom/cons x1 '()))
               ((1 E A Q) (atom/cons x2 '()))
               ((1 E E Q) (atom/cons x2 (cons x1 '())))

               ((1 A A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 A E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))
               ((1 E A) (if-false 'nil
                                  (if (equal (car (cons x2 '())) '?)
                                      't
                                      (memb? (cdr (cons x2 '()))))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x2 (cons x1 '()))) '?)
                                      't
                                      (memb? (cdr (cons x2 (cons x1 '())))))))

               ((1 A E Q 1) (car/cons x1 '()))
               ((1 A E E 1) (cdr/cons x1 '()))
               ((1 E A Q 1) (car/cons x2 '()))
               ((1 E A E 1) (cdr/cons x2 '()))
               ((1 E E Q 1) (car/cons x2 (cons x1 '())))
               ((1 E E E 1) (cdr/cons x2 (cons x1 '())))

               ((1 A E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))
               ((1 E A) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x2 '?)
                                   't
                                   (memb? (cons x1 '()))))

               ((1) (if-same (equal x1 '?)
                             (if (equal x2 '?)
                                 (if (equal x1 '?)
                                     'nil
                                     (memb? '()))
                                 (if (equal x1 '?)
                                     (memb? '())
                                     (memb? (cons x1 '()))))))

               ((1 A A) (if-nest-A (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 A E) (if-nest-A (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))
               ((1 E A) (if-nest-E (equal x1 '?)
                                   'nil
                                   (memb? '())))
               ((1 E E) (if-nest-E (equal x1 '?)
                                   (memb? '())
                                   (memb? (cons x1 '()))))

               ((1 A E) (memb? '()))
               ((1 E A) (memb? '()))
               ((1 E E) (memb? (cons x1 '())))

               ((1 A E Q) (atom '()))
               ((1 E A Q) (atom '()))
               ((1 E E Q) (atom/cons x1 '()))

               ((1 A E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E A) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))
               ((1 E E) (if-false 'nil
                                  (if (equal (car (cons x1 '())) '?)
                                      't
                                      (memb? (cdr (cons x1 '()))))))

               ((1 A) (if-same (equal x2 '?) 'nil))

               ((1 E E Q 1) (car/cons x1 '()))
               ((1 E E E 1) (cdr/cons x1 '()))

               ((1 E E) (if-nest-E (equal x1 '?)
                                   't
                                   (memb? '())))

               ((1 E E) (memb? '()))

               ((1 E E Q) (atom '()))
               
               ((1 E E) (if-true 'nil
                                 (if (equal (car '()) '?)
                                     't
                                     (memb? (cdr '())))))

               ((1 E) (if-same (equal x2 '?) 'nil))

               ((1) (if-same (equal x1 '?) 'nil))
               (() (equal 'nil 'nil))
               
               )))



